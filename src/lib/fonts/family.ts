// import { createRoot } from '../../utils';

// const variables = {
// 	'font-title': 'Roboto',
// 	'font-body': 'Roboto'
// };

export const fontsFamily = () => {
	const css = '';
	//css += createRoot({ variables });
	return css;
};
